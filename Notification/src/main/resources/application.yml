server:
  port: 0

spring:
  sendgrid:
    api-key: ${SENDGRID_API_KEY}

  data:
    mongodb:
      uri: ${MONGODB_URI}
      database: todolist-db

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVICE_URL}

